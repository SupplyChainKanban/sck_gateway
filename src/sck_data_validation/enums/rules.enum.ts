export enum Rules {
    required = 'required',
    numeric = 'numeric',
    number = 'number',
    alphanumeric = 'alphanumeric',
    date_format = 'date_format',
    optional = 'optional',
    enum = 'enum',
    string = 'string',
}

export const rulesList = [
    Rules.required,
    Rules.numeric,
    Rules.number,
    Rules.alphanumeric,
    Rules.date_format,
    Rules.optional,
    Rules.enum,
    Rules.string,
]